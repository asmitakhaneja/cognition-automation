# Devin Observability Dashboard — backend configuration
#
# Leave DEVIN_API_KEY unset to run on the built-in demo dataset.
# Set it to load live enterprise data.
#
# Supported credentials (sent as `Authorization: Bearer <key>`):
#   - v3 service-user token   (cog_...)     with the `ViewAccountConsumption` permission
#   - enterprise-admin key    (apk_user_...)
DEVIN_API_KEY=

# Optional overrides
# DEVIN_API_BASE_URL=https://api.devin.ai
# DEVIN_API_TIMEOUT=30
# DEVIN_DASHBOARD_FORCE_MOCK=0
